@model IEnumerable<Frycz_pcdb.computer>

@{
    ViewBag.Title = "List of Computers";
}
<div class="container">
<h1 class="jumbotron-heading text-center">List of Computers</h1>

<table class="table table-hover">
    <tr>
        <th scope="col">
            @Html.DisplayNameFor(model => model.name)
        </th>
        <th scope="col">
            @Html.DisplayNameFor(model => model.comment)
        </th>
        <th  scope="col">
            @Html.DisplayNameFor(model => model.idcomputer_type)
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.comment)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idcomputer_type)
        </td>
    </tr>
}

</table>
</div>